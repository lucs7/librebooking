{% for column in Definition.GetColumnHeaders() %}{% if ReportCsvColumnView.ShouldShowCol(column, loop.index0) %}"{% if column.HasTitle() %}{{ column.Title() }}{% else %}{{ translate(column.TitleKey()) }}{% endif %}",{% endif %}{% endfor %}{{ '\n' }}{% for row in Report.GetData().Rows() %}{% for data in Definition.GetRow(row) %}{% if ReportCsvColumnView.ShouldShowCell(loop.index0) %}"{{ data.Value()|escape }}",{% endif %}{% endfor %}{{ '\n' }}{% endfor %}
