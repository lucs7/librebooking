{% autoescape false %}username,email,first name,last name,password,phone,organization,position,timezone,language,groups,status{% for attribute in attributes %},{{ attribute.Label()|replace({("'"): "\\'"}) }}{% endfor %}{% endautoescape %}