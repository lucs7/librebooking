{% include 'globalheader.twig' %}

<div id="page-import" class="admin-page">

    <div class="card shadow col-12 col-sm-8 mx-auto">
        <div class="card-body">
            <h1 class="border-bottom mb-3">{{ translate('Import') }}</h1>
            <div class="text-center">
                <a href="ics_import.php" class="btn btn-primary me-4">{{ translate('ImportICS') }}</a>
                <a href="quartzy_import.php" class="btn btn-primary">{{ translate('ImportQuartzy') }}</a>
            </div>
        </div>
    </div>
</div>

{{ render_partial('javascript-includes.tpl', _context) }}
{% include 'globalfooter.twig' %}
