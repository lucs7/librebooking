{% extends 'layouts/base.twig' %}

{% block content %}
  <div class='error text-center'>
    <h3>{{ translate(ErrorMessage) }}</h3>
    <h5>
      <a
        class='link-primary'
        href='//{{ server.HTTP_HOST }}{{ server.REQUEST_URI }}'
      >
        {{- translate('ReturnToPreviousPage') -}}
      </a>
    </h5>
  </div>

  {{ include('javascript-includes.twig') }}
{% endblock %}
